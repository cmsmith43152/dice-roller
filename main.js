const rollButton = document.querySelector('#roll-button')
const rollNumber = document.querySelector('')

let dieRolls = []
let total = 0
let var = []


rollButton.addEventListener("click", function() {

    console.log("roll-button")

    let numberOfrolls = rollNumber.value
    console.log (numberOfrolls)

    //loop a number of times equal to the number-of-dice//
    //up to on click, you need to get the value from the text input box//
    //get the value of the input text box and assign it//
    // loop that number of times. 
    // and each time "roll" randomly from 1 to 6
    //loop a number of times equal to the number-of-dice
    


})
